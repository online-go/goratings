language: python

python:
  - 3.7.2

build:
  pre_ci_boot:
    image_name: u16pytall

  ci:
    - pip install -r requirements.txt
    - make shippable-test
    - make shippable-lint

  cache: true

  cache_dir_list:
    - $SHIPPABLE_BUILD_DIR/.venv
